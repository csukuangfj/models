name: vits-piper-de_DE-thorsten-high

on:
  push:
    branches:
      - download
  workflow_dispatch:

jobs:
  vits-piper-de_DE-thorsten-high:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate model
        shell: bash
        run: |
          cd .github/scripts
          export TYPE=high
          ./vits-piper-de_DE-thorsten.sh

      - name: upload model to huggingface
        shell: bash
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          cd .github/scripts
          ls -lh
          git clone https://huggingface.co/csukuangfj/vits-piper-de_DE-thorsten-high
          cp *onnx* ./vits-piper-de_DE-thorsten-high
          cp german.7z ./vits-piper-de_DE-thorsten-high
          cp lexicon.txt ./vits-piper-de_DE-thorsten-high
          cp tokens.txt ./vits-piper-de_DE-thorsten-high
          cp MODEL_CARD ./vits-piper-de_DE-thorsten-high
          cd ./vits-piper-de_DE-thorsten-high

          git lfs track "lexicon.txt"

          git config --global user.email "csukuangfj@gmail.com"
          git config --global user.name "Fangjun Kuang"

          git status
          git add .
          git commit -m "update"
          git push https://csukuangfj:$HF_TOKEN@huggingface.co/csukuangfj/vits-piper-de_DE-thorsten-high main

          rm -rf vits-piper-de_DE-thorsten-high/.git
          mv vits-piper-de_DE-thorsten-high ../..
          sudo apt-get install -y tree
          tree ./vits-piper-de_DE-thorsten-high
          ls -lh ./vits-piper-de_DE-thorsten-high

      - uses: actions/upload-artifact@v3
        with:
          name: vits-piper-de_DE-thorsten-high
          path: vits-piper-de_DE-thorsten-high
